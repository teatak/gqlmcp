# gqlmcp

mcp graphql proxy


```yaml
name: booking
version: 0.0.1
schema: v1
mcpServers:
  - name: booking
    command: gqlmcp/bin/gqlmcp
    env: 
    #   URL: "http://localhost:8201/api"
    #   HEADER: "Authorization: Token mytoken"
```